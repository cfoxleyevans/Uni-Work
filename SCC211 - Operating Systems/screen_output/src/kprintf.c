#include "globals.h"
#include <stdarg.h>

static char *
_itoa (unsigned int n, int sign, unsigned int base) {

   return p;
}

char *
itoa ( int i ) { return _itoa(i, 1, 10); }

int
strlen (char * str) {
   char * p = str;
   if (str) { }
   else return 0;
}

static void
outstr (char * str, int length, char pad) {
}

void
kprintf (char * format, ...) {
}
